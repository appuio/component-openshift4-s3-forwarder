parameters:
  kapitan:
    compile:
      - input_paths:
          - openshift4-s3-forwarder/component/app.jsonnet
        input_type: jsonnet
        output_path: .
      - input_paths:
          - openshift4-s3-forwarder/component/main.jsonnet
        input_type: jsonnet
        output_path: openshift4-s3-forwarder/${_instance}/
